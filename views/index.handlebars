<h1>Eat-Da-Burger!</h1>

<h2>Enter a burger you would like to submit!</h2>

<ul>
    {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block eaten=true}} {{/unless}} {{/each}}
</ul>

<h2>Burgers to devour:</h2>

<ul>
    {{#each burgers}} {{#if readyToEat}} {{> burgers/burger-block eaten=false}} {{/if}} {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
        <label for="devoured">Devoured?</label>
        <br>
        <input type="radio" name="devour" value="1" checked> Devoured!
        <br>
        <input type="radio" name="readyToEat" value="0"> Ready to Eat
    </div>

    <button type="submit">Add Burger</button>
</form>